services:
  - type: web
    name: django-api
    runtime: python
    buildCommand: "./build.sh"
    startCommand: "gunicorn stock.wsgi:application"
    envVars:
      - key: PYTHON_VERSION
        value: 3.12